/*
 * content fetcher
 * fetch article thumbnails of specific tags
 */
$(document).ready(function(e) {
  /*
  $.post('/archive/get', {filter: "天文学"}, function(data){
  if (data.err)
    alert(data.err);
  if (data.content)
    $('.sec_astronomy .thumbnails').empty().append(data.content);
  }, 'json');
  
  $.post('/archive/get', {filter: "心理学"}, function(data){
  if (data.err)
    alert(data.err);
  if (data.content)
    $('.sec_psychology .thumbnails').empty().append(data.content);
  }, 'json');
  
  $.post('/archive/get', {filter: "网络安全"}, function(data){
  if (data.err)
    alert(data.err);
  if (data.content)
    $('.sec_internetsecurity .thumbnails').empty().append(data.content);
  }, 'json');
  
  $.post('/archive/get', {filter: "诺贝尔奖"}, function(data){
  if (data.err)
    alert(data.err);
  if (data.content)
    $('.sec_nobelprize .thumbnails').empty().append(data.content);
  }, 'json');
  */
});